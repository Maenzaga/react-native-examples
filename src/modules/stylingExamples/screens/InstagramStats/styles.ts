import {StyleSheet} from 'react-native';

export default StyleSheet.create({
  sideImageInfo: {
    alignItems: 'center',
    padding: 8,
  },

  statValue: {
    fontSize: 14,
    fontWeight: 'bold',
    marginBottom: 4,
  },

  stat: {
    fontSize: 10,
  },
});
